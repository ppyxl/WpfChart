<UserControl x:Class="LcChart.LcLineChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" >
    <Grid>
        <Canvas x:Name="CoordinatePanel" ></Canvas>
        <Canvas x:Name="LinePanel" ></Canvas>
        <Canvas x:Name="MarkPanel" ></Canvas>
        <Label x:Name="MainLabel" FontSize="15" Margin="0,5,10,0" HorizontalAlignment="Right" VerticalAlignment="Top"></Label>
        <Border x:Name="MainBorder" Background="Transparent" MouseEnter="MainPanel_MouseEnter" MouseLeave="MainPanel_MouseLeave" MouseMove="MainPanel_MouseMove"></Border>
    </Grid>
</UserControl>
