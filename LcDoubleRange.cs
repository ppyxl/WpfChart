namespace LcChart
{
    public struct LcDoubleRange
    {
        public double Min = 0;
        public double Max = 0;

        public LcDoubleRange()
        { }

        public LcDoubleRange(double min, double max)
        {
            Min = min;
            Max = max;
        }
    }
}
